# UX Plan

### Task 1: Build login screen
**Files:**
- Create: `LoginView.swift`

**UX ref:** UX-001
**User interaction:** User taps "Sign In" and sees the login form animate up from the bottom.

**Verify:**
Run: `swift build`
Expected: builds

### Task 2: Build signup flow
**Files:**
- Create: `SignupView.swift`

**UX ref:** UX-002, UX-003
**User interaction:** User fills the signup form and taps "Create Account".

**Verify:**
Run: `swift build`
Expected: builds

### Task 3: Add analytics logging
**Files:**
- Modify: `AnalyticsLogger.swift`

**Verify:**
Run: `swift build`
Expected: builds
