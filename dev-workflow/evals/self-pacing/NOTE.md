Not observable: interactive
Spec: `skills/self-pacing/eval.md`
The skill is `disable-model-invocation: true` and its whole output is a pointer printed in the same turn — nothing beyond the static file text to grade in a non-interactive run.
