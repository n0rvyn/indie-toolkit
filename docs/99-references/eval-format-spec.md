# Eval.md Format Specification

Moved to `skill-master/skills/plugin-master/eval-rules.md` (2026-09-12) — it now ships with the plugin that enforces it.

That file defines the three `eval.md` forms (pointer / mixed / spec), the `evals/<skill>/` case layout, and the closed list of `Not observable:` reasons. Research and verification log: `docs/99-references/claude-plugin-eval.md`.
