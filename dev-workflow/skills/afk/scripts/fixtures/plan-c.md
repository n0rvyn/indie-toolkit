# Plan C — unrelated plan (fixture, must never be swept via --dev-guide alone)

### Task 1: Unrelated task

**Files:**
- `src/unrelated.py`

**Steps:**
1. do it

**Verify:**
Run: `true`
Expected: exit 0.

## Decisions

### [DP-001] Unrelated blocking choice? (blocking)

**Context:** open on purpose — this plan is out of scope unless named via --plans.
**Options:**
- A: yes
- B: no
**Recommendation:** A — reason.
