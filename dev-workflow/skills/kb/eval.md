# kb Eval

Cases: `evals/kb/` — run with `claude plugin eval ./dev-workflow --tag kb --scaffold --no-publish`. This file holds no assertions.

The behavior cases seed a fixture knowledge base with `scaffold_script`; without `--scaffold` they run against an empty one and fail.
